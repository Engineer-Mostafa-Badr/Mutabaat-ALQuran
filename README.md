# متابعه القرآن الكريم(Mutabaat-ALQuran)

> تطبيق مساعد لختم ومتابعة القراءة مع اختبارات مخصصة وجدول أسبوعي من دون تعقيد، مصمم لأداء سلس على شاشات متعددة ودعم تعدد اللغات.

---

## 🪔 نظرة عامة

متابعات القرآن ينظّم روتينك اليومي مع القرآن عبر وحدتين رئيسيتين تشتغلان معاً:

1.  **اختبارات مرنة** تتيح لك إنشاء أسئلة من سور أو آيات محددة، حفظ القوالب المفضلة، ومراجعة النتائج على طول.
2.  **مخطط أسبوعي وتتبع النشاطات** يعتمد التقويم الهجري أو الميلادي لتسجيل القراءة، الحفظ، المراجعة أو أي نشاط خاص بك، ثم عرض التزامك الشهري.
    إضافةً إلى ذلك، تُخزِّن جميع البيانات محلياً في قاعدة Drift، مع خدمات مخصصة لحفظ القوالب، نتائج الاختبارات، والتقارير اليومية.

---

## 🚀 المزايا

- **اختبارات قابلة للتخصيص**: اختَر سوراً أو نطاق آيات، احفظ قوالب جاهزة، واطّلع على سجل النتائج مع تصفية حسب التاريخ والنسبة.
- **جدول تفاعلي**: رتب أي يوم لأي نوع نشاط، سجِّل ما قرأته، وارجع للاطّلاع على إحصاءات الالتزام من داخل جدول الشهر.
- **قاعدة بيانات محلية غنية**: كل الكيانات (الأنشطة، القوالب، النتائج، المواعيد) موزعة في جداول Drift مع دعم التحميل الفوري.
- **طبقات خدمة مصممة**: خدمات منفصلة لإدارة الأنشطة، النماذج، النتائج، والقوائم تساعد في إعادة استخدام المنطق من شاشة إلى أخرى.
- **دعم لغات متعددة**: ملفات التعريب تشمل العربية والإنجليزية والبنغالية (أو الأردية) وغيرها، مع واجهة تُحمّل النص من assets/localizations.
- **واجهة نظيفة وقابلة لإعادة الاستخدام**: حِزم من الأزرار، الحقول، الحاجز، والقطاعات (segments) تضمن اتساق التصميم في كل الشاشة.

---

## 🏗️ البنية المعمارية

المشروع يعتمد على نمط Stacked (MVVM) مع فصل واضح بين الطبقات:

```
lib/
├── app/               # إعداد المسارات، الـlocator، الـdialogs، وتهيئة التطبيق
├── data/
│   ├── models/         # كيانات مثل Activity, Result, Template, Entry, Answer
│   └── tables/         # جداول Drift المتعلقة بالأنشطة، الآيات، القوالب، النتائج
├── services/           # خدمات Drift وSharedPreferences، والوظائف المساعدة مثل localization
├── surahs.dart        # ملف تحويلي لمعلومات السور
├── date_utils.dart    # دوال للتعامل مع التواريخ الهجرية والميلادية
└── ui/
    ├── common/         # ألوان، أزرار، حقول، شريط تنقل، مساعدات واجهة
    ├── dialogs/        # المربعات الحوارية والموديلات المصاحبة
    └── views/          # شاشات العرض مثل home, table, activity, results, test, main, startup
```

كل شاشة تتبع ViewModel مستقل من Stacked، ويستخدِم Services وRepositories المشتركة للوصول للداتا.

---

## 🛠️ التقنيات الرئيسية

- **Flutter (3.x)** مع ملف `pubspec` يضم Stacked، drift، وstacked_services.
- **State Management** عبر Stacked ViewModels و `stacked_generator`.
- **قاعدة البيانات**: Drift + sqlite3_flutter_libs لإدارة الجداول المحلية.
- **تخزين**: Shared Preferences للخيار السريع، مع خدمات محلية مخصصة.
- **تصميم**: Cairo/Poppins + `shimmer_animation`, `flutter_svg`, `iconly` لعناصر متطورة.
- **تواريخ**: `intl`, `jhijri`, `jhijri_picker` لدعم التاريخ الهجري.
- **مساعدات**: `collection`, `rxdart`, `url_launcher` للروابط والخدمات الفرعية.

---

## 📦 التشغيل المحلي

```
flutter clean
flutter pub get
flutter run
```

- لإعادة توليد الكود (stacked أو Drift) شغّل `flutter pub run build_runner build --delete-conflicting-outputs`.
- إذا أردت إنشاء الـ APK النهائي، استخدم:

```
flutter build apk --split-per-abi
```

---

## 📝 ملاحظات تطويرية

- الكود منظّم بعناية لمستوى Senior، مع فصل واضح بين المنطق والمظهر.
- كل ViewModel يربط الشاشة بـ Services/Repositories بحيث تبقى الشاشات خالية من التعقيد.
- التعريب يُحمّل من ملفات `assets/localizations`، ويمكن إضافة لغات إضافية بسرعة.
- جميع الثوابت (ألوان، نصوص، أبعاد) موجودة في `ui/common` لتسهيل التعديل.
